import React, { Fragment } from 'react';
import './Title.css';

const Title = () => {
  return(
    <div id="title">
      <h1>DeckFest Unlimited</h1>
      <em>This tool is why internet matters.</em>
    </div>
  );
};

export default Title;